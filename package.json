{
    "name": "ramda-doc",
    "version": "1.0.0",
    "private": true,
    "workspaces": ["vendor/*"],
    "scripts": {
        "build": "properdocs build",
        "publish": "properdocs gh-deploy",
        "postinstall": "rm -rf node_modules",
        "serve": "properdocs serve --open",
        "setup": "./scripts/setup.nu"
    }
}
